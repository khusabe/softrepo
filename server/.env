# База (SQLite файл в папке server)
DATABASE_URL="file:./dev.db"

# JWT для админ-авторизации (замените на длинную случайную строку)
JWT_SECRET="change_me_dev_secret"

# Порт сервера
PORT=8080

# Папка хранения файлов (относительно корня проекта server)
UPLOADS_DIR="uploads/software"

# Белый список IP (через запятую). Пусто — без ограничения.
ALLOWLIST_IPS=""